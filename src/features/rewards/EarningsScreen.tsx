import { PlaceholderScreen } from '@/shared/ui/PlaceholderScreen';

/**
 * M7 — Rewards & Earnings (Machine B).
 * Accumulated total, per-submission rewards, pending rewards, payout status
 * display (payout rails are Post-MVP).
 */
export function EarningsScreen() {
  return (
    <PlaceholderScreen
      title="Earnings"
      deliverable="Accumulated total, per-submission rewards, pending rewards, payout status (M7)."
      owner="Machine B — post-submit side"
    />
  );
}
